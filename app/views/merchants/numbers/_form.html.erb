<div class="row">
  <div class="col-md-7">
    <%= bootstrap_simple_form_for([ :merchants, @number ],url: merchants_order_numbers_path) do |f| %>
      <%= f.error_messages %>

      <%= f.input :pwd_number_default %>

      

      <%= f.button :submit, "确定", class: "btn-primary pull-right" %>
    <% end %>
  </div>
</div>
